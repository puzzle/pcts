import { ShowIfAdminDirective } from './show-if-admin.directive';

describe('ShowIfAdminDirective', () => {
  it('should create an instance', () => {
    const directive = new ShowIfAdminDirective();
    expect(directive)
      .toBeTruthy();
  });
});
