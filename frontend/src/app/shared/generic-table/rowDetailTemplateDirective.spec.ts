import { RowDetailTemplateDirective } from './rowDetailTemplateDirective';

describe('RowDetailTemplateDirective', () => {
  it('should create an instance', () => {
    const directive = new RowDetailTemplateDirective();
    expect(directive)
      .toBeTruthy();
  });
});
