---
services:
  pcts-backend:
    build:
      context: ..
      dockerfile: ./backend/Dockerfile
    environment:
      SPRING_PROFILES_ACTIVE: e2e-test

  pcts-frontend:
    build:
      args:
        BUILD_CONFIGURATION: development
