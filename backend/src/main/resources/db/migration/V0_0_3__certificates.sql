CREATE TABLE IF NOT EXISTS certificate
(
    id              BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    name            TEXT,
    points          NUMERIC NOT NULL CHECK (points >= 0 AND points = ROUND(points, 2)),
    deleted_at      TIMESTAMP DEFAULT NULL,
    comment         TEXT
)
